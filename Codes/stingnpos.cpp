    if(temp.find(s2) != string::npos)
    return true;

    //Here npos represent as no position , (-1) , False ,Here we trying to find s2 in temp string ,so if that != npos,
    //that means our s2 is present in temp string ;
    //Alse npos check the s2 till the end of the string temp; 